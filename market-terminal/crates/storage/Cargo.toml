[package]
name = "storage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Локальное аналитическое хранилище (DuckDB): схема, ингест, запросы"

[dependencies]
domain = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

# В фазе хранилища добавляется крейт `duckdb` (bundled). До этого здесь живут
# схема (DDL) и контракты запросов, не требующие нативной библиотеки.
