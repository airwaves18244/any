namespace RevitNvf.UI
{
    // Скаффолдинг (шаг 1). WPF dockable-панель параметров FacadeSystem
    // и viewmodels добавляются на шаге 8 (см. docs/ROADMAP.md).
    internal static class AssemblyMarker
    {
    }
}
