<UserControl x:Class="RevitNvf.UI.Views.FacadeParametersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignWidth="280">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,2,0,6"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
        <StackPanel>
            <TextBlock Text="Пресет системы" FontWeight="Bold"/>
            <ComboBox Margin="0,2,0,8"
                      ItemsSource="{Binding Presets}"
                      DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedPreset}"/>

            <TextBlock Text="Кронштейны (мм)" FontWeight="Bold"/>
            <TextBlock Text="Шаг по X"/>
            <TextBox Text="{Binding BracketStepX, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Шаг по Y"/>
            <TextBox Text="{Binding BracketStepY, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Отступ от края X"/>
            <TextBox Text="{Binding BracketEdgeOffsetX, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Отступ от края Y"/>
            <TextBox Text="{Binding BracketEdgeOffsetY, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Облицовка (мм)" FontWeight="Bold"/>
            <TextBlock Text="Ширина элемента"/>
            <TextBox Text="{Binding PanelWidth, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Высота элемента"/>
            <TextBox Text="{Binding PanelHeight, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Ширина шва"/>
            <TextBox Text="{Binding JointWidth, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Привязка"/>
            <ComboBox Margin="0,2,0,6" ItemsSource="{Binding StartModes}" SelectedItem="{Binding StartMode}"/>
            <CheckBox Margin="0,2,0,8" Content="Доборные элементы по краям"
                      IsChecked="{Binding IncludeEdgePanels}"/>

            <TextBlock Text="Пирог (мм)" FontWeight="Bold"/>
            <TextBlock Text="Утеплитель"/>
            <TextBox Text="{Binding InsulationThickness, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Воздушный зазор"/>
            <TextBox Text="{Binding AirGapThickness, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Толщина облицовки"/>
            <TextBox Text="{Binding CladdingThickness, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
