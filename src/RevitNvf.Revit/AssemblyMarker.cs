namespace RevitNvf.Revit
{
    // Скаффолдинг (шаг 1). Точка входа IExternalApplication (RibbonApp),
    // вкладка на ленте и команды добавляются на шаге 2 (см. docs/ROADMAP.md).
    internal static class AssemblyMarker
    {
    }
}
